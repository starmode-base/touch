{
  "name": "touch-chrome",
  "private": true,
  "scripts": {
    "dev": "bun build ./*.ts --outdir ./ --watch",
    "build": "bun build ./*.ts --outdir ./",
    "typecheck": "tsc",
    "clean:soft": "rm -rf node_modules tsconfig.tsbuildinfo background.js content-script.js && bun install",
    "clean:hard": "rm -rf node_modules tsconfig.tsbuildinfo background.js content-script.js bun.lock && bun install"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.27",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "zod": "^4.1.12"
  }
}
