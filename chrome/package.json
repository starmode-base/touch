{
  "name": "touch-chrome",
  "private": true,
  "scripts": {
    "chrome:dev": "bun build ./*.ts --outdir ./ --watch",
    "chrome:build": "bun build ./*.ts --outdir ./",
    "typecheck": "tsc"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.9",
    "typescript": "^5.9.2"
  }
}
